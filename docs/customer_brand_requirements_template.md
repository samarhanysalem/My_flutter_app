Customer Brand Requirements
Fill this out for each new customer. Once complete, hand it to Claude Code with: "Apply this brand config to app_config.dart, app_theme.dart, and assets/branding/."

1. App identity

* App display name:
* Clinic / company name (shown in-app, e.g. headers, about):
* Package name / bundle ID (e.g. `com.clinicname.appointments` — must be unique per app store submission):
* Support email:
* Support phone (optional):

2. Visual branding

* Logo file (attach — PNG/SVG, transparent background preferred):
* App icon (attach — square, min 1024x1024):
* Primary color (hex):
* Accent color (hex):
* Any secondary/tertiary colors used in their existing branding (hex, optional):
* Font preference (if they have brand guidelines; otherwise default app font is used):

3. Firebase / backend

* New Firebase project created? (yes — provide project ID / config, or "no, please create one for me")
* Firestore data to pre-load, if any (e.g. their doctor list, if not empty at launch):

4. Store listing (if customer is publishing under their own name)

* Apple Developer account access: (customer's own account / needs to create one)
* Google Play Developer account access: (customer's own account / needs to create one)
* Store listing description (short text for the app store page):
* Screenshots needed: (yes, generate from app / customer will provide)

5. Feature toggles (if applicable)

* [ ] Standard booking flow only
* [ ] Multiple clinic locations (if they have more than one branch)
* [ ] Other requested customization: ______________________

6. Notes / anything else specific to this customer
(free text)
